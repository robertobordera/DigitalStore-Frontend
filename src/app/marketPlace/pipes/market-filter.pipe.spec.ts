import { MarketFilterPipe } from './market-filter.pipe';

describe('MarketFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MarketFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
