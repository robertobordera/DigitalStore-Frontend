import { BmAutosuggestDirective } from './bm-autosuggest.directive';

describe('BmAutosuggestDirective', () => {
  it('should create an instance', () => {
    const directive = new BmAutosuggestDirective();
    expect(directive).toBeTruthy();
  });
});
