import { BmMapDirective } from './bm-map.directive';

describe('BmMapDirective', () => {
  it('should create an instance', () => {
    const directive = new BmMapDirective();
    expect(directive).toBeTruthy();
  });
});
