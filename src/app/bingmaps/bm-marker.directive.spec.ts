import { BmMarkerDirective } from './bm-marker.directive';

describe('BmMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new BmMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
